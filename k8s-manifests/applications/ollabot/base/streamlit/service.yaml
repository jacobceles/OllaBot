apiVersion: v1
kind: Service
metadata:
  name: streamlit
  labels:
    app: streamlit
spec:
  type: NodePort
  selector:
      app: streamlit
  ports:
  - name: streamlit
    port: 8501
    targetPort: 8501
  - port: 8000
    targetPort: 8000
    name: metrics